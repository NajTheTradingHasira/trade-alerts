name: Daily Market Close Scan

on:
  schedule:
    # 4:30 PM EDT = 20:30 UTC (during EDT, Mar-Nov)
    # During EST (Nov-Mar), this fires at 3:30 PM ET — still fine, market closes at 4 PM
    - cron: '30 20 * * 1-5'
  workflow_dispatch:
    inputs:
      skip:
        description: 'Screeners to skip (space-separated: 2020 4040 ep bullsnort volume rs postmarket)'
        required: false
        default: ''
      only:
        description: 'Run only these screeners (space-separated)'
        required: false
        default: ''

jobs:
  daily-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install yfinance pandas numpy slack-sdk requests

      - name: Run daily scan
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          POLYGON_API_KEY: ${{ secrets.POLYGON_API_KEY }}
          PYTHONIOENCODING: utf-8
        run: |
          ARGS="--slack --config channel_config.json"
          if [ -n "${{ github.event.inputs.skip }}" ]; then
            ARGS="$ARGS --skip ${{ github.event.inputs.skip }}"
          fi
          if [ -n "${{ github.event.inputs.only }}" ]; then
            ARGS="$ARGS --only ${{ github.event.inputs.only }}"
          fi
          python daily_scan.py $ARGS

      - name: Upload CSV results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: scan-results-${{ github.run_number }}
          path: |
            *.csv
            output/*.csv
          retention-days: 30
          if-no-files-found: ignore
